#!/usr/bin/python3
def no_c(my_string):
    if my_string is none:
        return
    new_string = ""
    length = len(new_string)
    for elm in range(length):
        new_string += new_string[elm]
    return new_string
